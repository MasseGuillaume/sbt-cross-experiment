package userland

object User {
  def main(args: Array[String]): Unit = {

// (X) user changes:

// plugins.sbt
    /*
    scala-js-cross 0.1.0   (1) add scala-js-cross
    scala-native   0.1.0   (2) add scala-native
    sbt-cross      0.1.0   (3) add sbt-cross
    */

// build.sbt
    // == generated by sbt ==
    // the ordering must not affect the result
    import sbt._
    import sbt.Keys._
    import sbtcross.AutoImports._
    import scalajs.AutoImports._
    import scalajscross.AutoImports._
    import scalanative.AutoImports._
    // == end generated by sbt ==

    // == user build ==

    // (4) shadowing scalajs crossProject
    import sbtcross.{crossProject, toScalaJSGroupID}

    // (5*) crossProject = deprecation warning
    crossProject

    crossProject.settings(
      // (6*) %%% = deprecation warning
      libraryDependencies += "org.example" %%% "foo-bar" % "0.1.0"
    )

    println(
      // (7) the user specifies the targets
      crossProject(JSPlatform, JVMPlatform, NativePlatform)
        .settings(libraryDependencies += "org.example" %% "foo-bar" % "0.1.0")
        .build
    )
  }
  // cross CrossVersion.full
}

